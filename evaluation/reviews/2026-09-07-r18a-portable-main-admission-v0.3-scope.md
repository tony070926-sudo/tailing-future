# R18a portable main admission /0.3 — frozen implementation scope

Frozen by root on 2026-09-07 before Astra implementation. This is a separate
worktree and versioned compatibility hypothesis, not an edit to the currently
running final-gate candidate and not a new scientific capability claim.

## Hypothesis

The unchanged R18a scientific source, locked runtime and formal receipt can
be admitted and executed on the specifically pinned upstream integration base
`a63415c174dcfaa0dc5370ecc777b6ccdcda86aa`, including an actual Git tree/index
checkout with ordinary source permissions, while preserving historical
evidence and honestly separating scientific-contract provenance from the
actual integration source identity.

This known base is not asserted to be the latest remote main. No remote fetch,
commit, push, deployment or protected-CI claim belongs to this implementation
scope. Release eligibility remains false until a separately verified complete
release gate allows it. Full t=8 convergence remains an outstanding scientific
requirement; this round does not replace that goal with compatibility work.

## Isolation and fixed inputs

- Writable Builder root:
  `/Users/tonywilliam/Documents/ChatGPT/Tailing Future-backend-release-r18a-portable`.
- Read-only previous integration:
  `/Users/tonywilliam/Documents/ChatGPT/Tailing Future-backend-release-r18a`.
- Read-only original:
  `/Users/tonywilliam/Documents/ChatGPT/Tailing Future`.
- The previous candidate is 25 files, 4209 canonical bytes,
  SHA256 `44b1e1d0c9f4208d87faa0b5dae49fa93302ea10217da1e7af59aae8e1e203a5`.
  Its 489-path full-gate input has SHA256
  `f5d3278137026fffe70cbbc9e86ca22e4442bdc6a4c029a0d467086c14970527`.
- Its final full gates are still running under
  `/private/tmp/tf-r18a-current-main-final-gates-5EGXGJ`.
  Do not mutate that candidate, snapshot or evidence; do not launch duplicate
  full gates. Root will provide its final review when it actually exists.
  No new candidate freeze or approval may pretend that pending review exists.
- The new Git base contains 452 regular 100644 files. Preserve all except the
  three explicitly reconciled configuration paths below. The full 13-path
  upstream delta is inherited together, byte-for-byte from the pinned Git
  commit. Its reports and review are historical inputs, not outputs of this
  new candidate's execution.
- Preserve all 641 original source identities and all nine excluded R12 paths
  in their original location. R12 is not admitted here. Exclusion is not
  clearance, licensing or a score increase.

## Exact change surface

1. Import the previous integration's non-main historical/admission/R18a files
   byte-identically, including both admission versions, original runners,
   scientific imports, notices, scope and review evidence. Do not overwrite
   any member of the upstream 13-file cluster with an older version.
   The previous final review is added only after root completes it.
2. Reconcile only `.gitignore`, `package.json` and `vitest.config.ts`.
   Retain the runtime exclusion and required Node29 dispatch. Route the new
   explicit numerical command to a versioned runner; unchanged old actual-tree
   admission tests must run in their correct mandatory historical trees.
   Do not change frontend-facing tf.world/action/observation /0.3 contracts,
   app/lib code, evaluator code, workflows, dependencies or lockfiles.
3. Add:
   - `schemas/backend-migration-admission-v0.3.schema.json`
   - `evaluation/backend-migration/r18a-origin-main-admission-v0.3.json`
   - `scripts/backend-migration/verify-r18a-origin-main-admission-v0.3.mjs`
   - `scripts/backend-migration/verify-r18a-origin-main-admission-v0.3.test.mjs`
   - `scripts/mesoscale/pfhub7a_r18a_current_root_v2.mjs`
   - `scripts/mesoscale/pfhub7a_r18a_current_root_v2.test.mjs`
   - `evaluation/backend-migration/r18a-v0.2-historical-config-snapshot-v0.1.json`
   - this scope and exactly enumerated dated manifest/Builder/science/software/
     SOTA/final review artifacts for this candidate.
4. The small historical config snapshot may contain only the exact three
   previous reconciled configuration files, with original UTF-8 bytes, modes,
   sizes and digests checked against the old ledger. Other historical inputs
   come from pinned Git blobs or unchanged digest-checked files, not a broad
   duplicate repository bundle. Do not patch old test or evidence bytes.
5. Freeze the final exact path set, content/mode policy and all control-file
   identities before independent review. Do not use a directory-prefix
   exemption, self-asserted score or approval transfer as admission.

## Acceptance, fixed before Builder implementation

A. Upstream and historical integrity

- Verify all 13 upstream delta paths against their pinned Git blobs; preserve
  the host-observation /0.2, determinism-root, observer, evaluator and test
  cluster together. Verify unchanged frontend contracts and other base files.
- Verify all old ledger/schema/checker/test and scientific bytes. The old
  original 0444 receipt remains untouched in both existing roots.
- Mandatory historical replay must actually execute the unchanged old /0.1
  and /0.2 tests in their correct reconstructed source trees with no skips.
  A historical replay is not a current-tree approval. Make reconstruction
  independent of absolute paths to either older worktree.
- Reject missing, extra, hidden, duplicate, changed, renamed R12 or unregistered
  source identities and any scientific/release/historical-approval escalation.

B. Git source permissions, not runtime sealing

- Record original receipt provenance as 0444, 248778 bytes and SHA256
  `d91e31d261bf77f99b17e810f5f51aab131dd5339e281839933e45fa79855e5c`.
- Record its Git source mode as 100644. For this one source path only, permit
  ordinary 0644 or historical 0444 POSIX mode. Require a regular non-symlink,
  single-link source, exact size/digest and stable reads. Reject execution,
  group/world-write and special-mode changes. Do not generalize this exception
  to other paths, private runtime files, private candidate snapshots or newly
  generated sealed receipts.
- In a separate empty directory, perform an actual Git tree/index checkout
  from a recorded candidate tree, with no post-checkout chmod correction.
  Record tree OID, index modes and actual checkout identities. This does not
  create an approved release commit or establish Linux CI.
- Execute the new admission and unchanged Node29 corpus in that checkout.
  Explicitly test content, executable-bit, unsafe permission, symlink/hardlink
  mutations and owner-write-only compatibility. Preserve original evidence.

C. Truthful executable identity and numerical acceptance

- Version the execution evidence. Separately record the historical
  scientificContractBaseCommit (307e6d7 full SHA), verified integrationBaseCommit
  (a63415c full SHA), and actual frozen candidate tree identity.
  Verify actual source content and the new admission before and after the
  execution. Do not merely rewrite the old runner's hardcoded base label or
  treat git HEAD as proof of an uncommitted candidate.
- Preserve original numeric equations, physical interpretation, 8x4/two-step
  dt=0.01/t=0.02 PFHub dimensionless acceptance, thresholds, runtime lock and
  all three tracks. No convergence, material or causal capability escalation.
- Preserve caller-supplied verified Darwin arm64 bootstrap/wheel inputs,
  monotonic aggregate numerical budget 600000 ms and failure-only stop grace
  1000 ms. Missing/wrong inputs, platform, deadline, identity, incomplete
  corpus, unconfirmed cleanup or receipt mismatch must produce nonzero
  machine-readable abstention. No dependency download or automatic resolver.
- The new explicit gate must actually pass Python21, Node29, strict JSON,
  schema and semantic validation without numerical skips, and execute two
  identical supervisor commands whose new receipts match the exact original
  248778-byte formal receipt. Private sealing policies stay unchanged.
- Test truthful evidence fields, stale integration/changed candidate identity,
  before/after drift and the retained deadline/final-budget behavior. A
  self-claimed base or candidate digest cannot make altered inputs pass.

D. Iteration and final gates

Builder uses apply_patch for source edits, performs self-checks and freezes
diff plus actual evidence; Builder cannot approve. Root owns subsequent fixes.
Independent science, software/numerical/evaluator and dated primary-source
SOTA reviewers remain read-only. P0/P1 blocks commit/promotion; relevant
reviewers must verify closure after fixes. Then run applicable lint, type,
full tests, numerical, schemas, evaluator and production build, recording
raw evidence, warnings/skips and limitations in a dated final review.

While the prior full-gate job is live, implementation and light syntax/static
checks may proceed here. Do not start another long CPU-bound numerical,
full-core or evaluator suite that could perturb its scientific timeouts.
Coordinate focused testing with root. Freeze no invented or pending gate
result. No commit, push, deployment, model-score increase or Goal completion
is authorized by this scope.
