# Full-candidate host observer contract — independent review

Date: 2026-09-04

Implementation disposition: **LIMITED GO only for the exact fixture-only,
unregistered and non-dispatchable H-vNext-Observer/0.1 contract reviewed here**

Model execution, scientific comparison, promotion, publication, runtime
redistribution, release and deployment disposition: **NO-GO**

The reviewed source freezes an execution definition, a same-job host-observer
contract, a not-run receipt schema, deterministic synthetic fixtures and
fail-closed evaluator wiring for a possible future MatterSim/MACE campaign.
It does not register an executable workflow, invoke either model, or produce
energy, force, stress, error, uncertainty, throughput, memory or GPU results.
No 693-by-2 inference was run and the evidence-maturity score remains 41/100.

This record is source-scoped. To avoid a self-referential hash, it records the
exact pre-record tree approved by the independent reviewers but cannot contain
the final tree, source-manifest digest or production artifact digest that
includes its own bytes. Acceptance requires a subsequent complete local gate
run, evaluator refresh, production rebuild and final freeze without editing
this file. Any later edit makes this review record stale.

## Frozen bounded hypothesis

The round tested one bounded hypothesis:

> Tailing Future can freeze a versioned, atomistic-only host-observer and
> receipt contract for four sequential fresh-container executions on one
> shared host, prove its arithmetic and fail-closed boundaries with synthetic
> fixtures, and hard-gate the evaluator while keeping every dispatch,
> scientific, publication and redistribution claim disabled.

The frozen future execution order contains, for each of MatterSim and MACE,
one authoritative ordinal-zero execution over all 693 Random-TP structures
and one fresh-container ordinal-one repeat-validation execution over the same
693 IDs. Each authoritative execution also plans 40 invariance cases,
89 force finite-difference cases with four energy probes each, and 60 stress
finite-difference cases with four energy probes each. The exact batch-one
adapter-request accounting is therefore:

```text
2 × 693 authoritative predictions
+ 2 × 693 repeat-validation predictions
+ 2 × 40 invariance probes
+ 2 × 89 × 4 force finite-difference energy probes
+ 2 × 60 × 4 stress finite-difference energy probes
= 4,044 requests
```

Probe records are excluded from the metric and determinism denominators. The
repeat execution cannot replace ordinal zero as scientific authority. These
are preregistered counts, not observations of an executed campaign.

Acceptance required all of the following:

- every scientific count to carry value, unit, dimension and basis, with
  per-execution and total arithmetic derived from the structured values;
- fractional translation and proper-rotation values to declare unit,
  dimension and basis;
- exact host assignment of sequence, model, model ID, ordinal, role,
  container, output directory and lifecycle identity, with explicit field
  provenance and no trust in container self-report;
- one-host sequential lifecycle ordering, distinct containers and writable
  outputs, network and label isolation, finite resource limits before any
  future dispatch, and no throughput or performance claim;
- calculator-native stress to come directly from
  `atoms.calc.results['stress']` immediately after calculation and before
  `Atoms.get_stress`, Voigt conversion, symmetrization or unit conversion;
- the raw native full 3-by-3 tensor to pass all-nine-entry antisymmetry testing
  at an inclusive absolute tolerance of `1e-10 eV/angstrom^3` and relative
  tolerance zero; native Voigt-6 must abstain because pre-conversion
  antisymmetry cannot be established;
- contract, unregistered workflow and both new schemas to be exact regular
  mode-0644 single-link files bound through `O_NOFOLLOW` descriptors, path
  identity, exact bytes, stage-zero mode-100644 Git blobs, computed blob IDs,
  one `write-tree`/`ls-tree` result, a final raw index reread and final
  filesystem identity;
- synthetic fixture objects to use closed allowlists and reject secret,
  reference-label, reference-energy and other unregistered leaves;
- private execution, aggregate publication and runtime redistribution to be
  three independent default-deny rights decisions;
- unresolved rights to produce `rights-not-cleared`, `not-run` and `abstain`;
- the fixture workflow to remain outside `.github/workflows`, unreachable,
  guarded by literal false, empty-permissioned, secret-free, artifact-free and
  unable to dispatch a container or model;
- every scientific and industrial claim to remain false; and
- no change to `tf.world/0.3`, `tf.action/0.3` or `tf.observation/0.3`, and no
  reinterpretation of the unchanged v0.2 full-candidate receipt boundary.

The Builder could implement and self-check the candidate but could not approve
it. The reviews below do not substitute for executable validation, a model
campaign, an independent solver, experiments, legal advice or qualified
engineering approval.

## Live baseline and conflict isolation

- Work was isolated in
  `/Users/tonywilliam/Documents/ChatGPT/Tailing Future-v05-observer` on
  `codex/v05-observer-contract`; the user's other dirty worktree was not
  modified.
- Local `HEAD` and the locally fetched `origin/main` were
  `dba1b96f96bb6a85cd82e83d55c7d86c92bb41c0` at both formal review freezes.
- Main Sentinel run `33819691863` completed successfully for that exact SHA;
  reporter run `33821989104` was skipped. Those are current-main observations,
  not candidate CI.
- Main protection required strict `evaluate`, enforced administrators and
  conversation resolution, required linear history, and rejected force pushes
  and deletions.
- The protected canonical Cloudflare site returned HTTP 200 with title
  `Tailing Future — 材料世界模型实验室`. This observed the previously deployed
  main site, not this candidate.

Before commit, the main agent must fetch remote main again and recheck overlap.
Local validation, commit, push, PR, candidate CI, merge, first-main CI, release
artifact and deployment remain separate lifecycle states.

## Frozen sources and contract identities

| Reviewed source | Exact identity |
| --- | --- |
| Observer contract | raw SHA-256 `9fd93ded20bd013a0c6d61dfec48368fc7957725ee4f95f4fafa242c7f7f0759`; semantic SHA-256 `9cd846d04f9d948ff5e0caf7c899f748f8191b73b56a44f943ee32952f6bddd3` |
| Unregistered fixture workflow | 641 bytes; raw SHA-256 `70732b76447b339decf5a1a8296e59c9186377f64bd4cfb59e81f4cd405f265c` |
| Contract schema | raw SHA-256 `cf3d5d3d3531d08f17df46f69a78590d656afd80dbbaf2db61bcad6cb9a1eb53` |
| Host-observation receipt schema | raw SHA-256 `8e9828456a73b9a8bf986e7e1365578b4c620957955fbd3a525a56e081aa90aa` |

The source binder records device, inode, mode, link count, size, nanosecond
modification time and nanosecond change time before/opened/after/final checks.
Mutation tests cover path replacement, worktree/index byte divergence, index
changes after the first read, staged-tree drift, final-index drift, wrong mode,
symlinks and hard links across all four reviewed paths.

The rights evidence projection binds these existing repository sources:

| Evidence | Raw SHA-256 | Bound fact |
| --- | --- | --- |
| `evaluation/data/datasets.json` | `ca6e7ca0187cf5584806f2684611b2b813b46bd7c434741d756df7947aef8fe7` | Random-TP dataset digest, `NOASSERTION` status and `redistribute:false` |
| `evaluation/atomistic/reproduction-plan.json` | `d3a58524029b51c598d00a7bb9f60b6479a9973a0f9907cbf94a31e61bf1c9c2` | MatterSim 1.2.5 and MACE 0.3.16 commits, MIT model/code declarations and checkpoint digests |
| `evaluation/atomistic/runtime-lock.json` | `b8c352aacfef3f74210d2dbf2002400887e35d21670f5f93da6a8003670bafa1` | unresolved runtime state and both dependency-lock digests |
| `atomistic/locks/README.md` | `65abe1d5c48185090a1b1a96cfa813f3b8eb88a25cf10f59798798db5ae36a03` | `python-hostlist==2.3.0` is GPL-2.0-or-later and distribution review remains unresolved |

These bindings record evidence and default-deny policy; they do not themselves
grant private dataset use, aggregate-result publication or runtime
redistribution rights.

## Candidate freezes

The first formal freeze against base `dba1b96...` was:

- staged tree `b5a9edaf7b2faaa0708cfb88054fc905bc19d2f2`;
- nine staged files with no unstaged or untracked files;
- cached binary patch 151,387 bytes; and
- patch SHA-256
  `1571f14ff1725db03b4614597bd974f6fa18e5e2a53ef39ab106306c867bb4c8`.

Independent review found no P0 but seven blocking P1 findings across the three
review scopes. The main agent fixed them, regenerated deterministic evaluator
outputs, reran targeted gates, and froze the repaired pre-record candidate as:

- staged tree `e504b873d3231d5db87422648723c4290f988c96`;
- 13 staged files with no unstaged or untracked files;
- cached binary patch 241,354 bytes; and
- patch SHA-256
  `70e3f0fae5599f480b95ac993f25cc595bd8a568d4503b8f360ae4d9b7545bde`.

All three independent closure reviews recomputed and approved only the second
identity. The post-record final tree must differ because it includes this file
and refreshed evaluator outputs; the opening lifecycle condition governs it.

## Independent reviewers

- Builder and self-check: Ampere. The Builder did not approve the candidate.
- Mechanism/scientific validity: initial reviewer
  `/root/science_review_observer`; closure was independently rechecked by
  `/root/round3_gap_planner/observer_numeric_audit`.
- Software, numerical and evaluator integrity, plus Gap Planner comparison:
  Feynman, `/root/round3_gap_planner`.
- Dated primary-source, SOTA and rights boundary: Erdos,
  `/root/sota_rights_review_observer`.

All review work was read-only. The main agent owned every repair. Reviewers
reported `P0=0`, `P1=0` on the repaired freeze and GO only for the bounded
fixture contract.

## Findings and dispositions

### P0

No P0 finding was recorded.

### P1 — all closed

1. **Raw stress could be silently symmetrized through ASE before the gate.**
   Closed by requiring the exact calculator-native source before
   `Atoms.get_stress` or Voigt conversion, testing all nine full-matrix
   entries, abstaining on native Voigt-6, and adding a regression in which ASE
   canonicalization would hide a rejected antisymmetric component.
2. **Fractional translation and proper rotation lacked unit, dimension and
   basis.** Closed by structured quantities in the contract and schema plus
   leaf-specific mutation tests.
3. **The four reviewed sources lacked one complete descriptor/index/tree
   binding.** Closed by the identity protocol and race corpus described above.
4. **Scientific counts were bare integers.** Closed by structured count
   quantities, `.value`-based derivation and mutations of value, unit,
   dimension and basis.
5. **Host ordinal authority was not auditable.** Closed by 36 required identity
   fields, per-field provenance, host-assigned execution identity, sequential
   monotonic lifecycle rules, unique container/output checks and rejection of
   self-reported authority.
6. **Synthetic records accepted unknown sensitive leaves.** Closed by exact
   allowlists at fixture, model, record, case and host levels, including direct
   rejection tests for `secret`, `referenceLabels` and `referenceEnergy`.
7. **Rights were not three independent default-deny gates.** Closed by
   `privateExecutionAllowed:false`, `aggregatePublicationAllowed:false` and
   `runtimeRedistributionAllowed:false` across contract, both schemas,
   fixture, receipt, semantic tests and evaluator hard gates, with
   `rights-not-cleared` forcing abstention.

### P2 — non-blocking and preserved

1. The observer policy is directly hard-gated and the score remained 41, but
   there is no dedicated source-snapshot mutation test proving that an
   observer-source alteration produces a Sentinel hard gate while leaving
   scorecard dimensions unchanged.
2. The raw-stress source and host lifecycle/resource observer are contracts
   and synthetic helpers only. The existing v0.2 central verifier still uses
   its historical `Number.EPSILON` symmetry rule. A real adapter requires a
   new versioned implementation, non-orthogonal numeric oracles and explicit
   compatibility tests; v0.2 must not be silently reinterpreted.
3. The `python-hostlist` license fact is directly bound to a frozen internal
   notice. A stronger future chain should additionally bind official versioned
   PyPI metadata, the 2.3.0 sdist digest and license bytes, and MACE
   `setup.cfg` at the pinned commit.

## Executable evidence before this record

| Gate | Result | Boundary |
| --- | --- | --- |
| Observer policy | PASS: 113/113 | Includes four-source Git races, quantity mutations, host mapping, sensitive-leaf rejection, raw 3-by-3/Voigt-6 and receipt abstention |
| Legacy preflight/v0.2 | PASS: 94/94 | Same-job receipt remains rejected under the unchanged historical contract |
| ESLint | PASS | Complete configured repository surface |
| TypeScript | PASS | `tsc --noEmit`, run alone after evaluator file restoration |
| Atomistic plan | PASS | `693×2 — NOT RUN`; fixture only; dispatch blocked; no inference |
| Sentinel | CONDITIONAL 41/100; no hard-gate failure | Evidence maturity only; 410-source manifest; source artifact `sha256:d12f8d64b51ed8c6c0bb44bd1cca04f22ebab37fd10df0c18ca01f10aac148fc` |
| Production build/isolation | PASS | 64 files, 5,549,854 bytes; digest `a47e16acc92f2f1fb59dbd1e06518aaafb3ce8843e6ecf6fd7099c39ff39cbbd`; eight forbidden public fields absent |
| Production dependency audit | PASS | Zero reported vulnerabilities |

Three non-candidate failures are retained rather than relabelled as passes:

- an early TypeScript invocation ran concurrently with the evaluator while it
  atomically refreshed public evaluation files; it observed those files
  temporarily absent. The same command passed when run alone;
- an early full suite omitted the required fixed `gh` path and correctly
  failed ten runtime-freeze environment checks; that invalid invocation was
  stopped; and
- a correctly configured full suite then found a pnpm-symlinked local
  Playwright package tree and one pre-existing 30-second V048 timeout under
  shared host load. `npm ci --ignore-scripts` reconstructed the exact
  `package-lock.json` npm layout; the Playwright preflight then passed 10/10 and
  the V048 file passed 3/3. A later shared-load run hit one existing
  thermochemical 20-second timeout; its file passed 22/22 when run alone.

These retries establish the local failure causes but do not replace the
mandatory post-record clean full run. That complete run, evaluator refresh,
production rebuild and final source freeze must follow this record.

## Scientific, product and rights boundary

The contract is atomistic-only. It introduces no cross-scale bridge, causal
effect, counterfactual, electronic density, orbital, bond order, learned
trajectory or new visualization. It does not describe the R2 reduced-unit
Lennard-Jones/heat/A-to-B world as a material, chemical mechanism, reactor or
industrial process. No automatic path to PLC, DCS, SIS or other
safety-critical control is added.

The four host observations in tests are synthetic fixtures. The emitted
receipt has `execution:not-run`, `scientificDecision:abstain`, zero observed
container executions, null measurements, unresolved resources, all claims
false and all three rights gates false. Fixture acceptance is contract-test
evidence, not model evidence.

MatterSim and MACE remain external `AUDITABLE` references. MatterSim's reported
Random-TP values of 0.199 eV/atom, 0.824 eV/angstrom and 1.999 GPa remain
external model-card results, not local `REPRODUCED` measurements or a ranking.
MACE has no locked official like-for-like Random-TP target. Upstream SOTA
language is a `CLAIM` for its own task and cannot enter this comparison.
AIDO Cell remains an architectural `REFERENCE`, not a like-for-like materials
baseline.

The dated rights/SOTA reviewer checked pinned primary sources and also
observed current upstream heads on 2026-09-04. The candidate deliberately
continues to bind the reviewed releases rather than silently advancing:

- MatterSim v1.2.5 model card and Random-TP source at
  `40a1eb8f1189a53af310957b4f2c5dfbfe68d647`;
- MACE v0.3.16 source and dependency declaration at
  `4d2da09413ac1407f37cdbb6b81fa28e4c15655e`;
- MPA-0 foundation license at
  `6de003bb29db05f451051c30ce809fad522d26da`; and
- GenBio Research Overview at
  `399e02e4e97222fdc44f091d48a1b297e52e6513`.

No data-leakage certification, broad materials validity, experimental
agreement, model superiority, SOTA, publication permission, redistribution
permission or industrial fitness claim is allowed.

## Lifecycle status at record creation

| State | Status |
| --- | --- |
| Local implementation | Complete for the bounded fixture contract; post-record complete gate pending |
| Commit | Not created |
| Push | Not performed |
| Pull request | Not created |
| Candidate CI | Not run |
| Main merge | Not performed |
| First-main CI | Current main `dba1b96...` passed; no candidate first-main CI exists |
| Release artifact | Not created for this candidate |
| Cloudflare deployment | Not performed |
| Canonical smoke | Existing deployed main observed only; not candidate evidence |

No commit, push, PR, merge, release or deployment is permitted unless the
post-record gates pass, the source/report/build identities are frozen, and a
fresh remote-main fetch shows no conflict. This round does not authorize a
Cloudflare deployment even if CI passes.

## Residual limitations and next round

The next round is limited to at most three tasks:

1. Add a source-snapshot observer mutation-to-hard-gate regression that also
   proves the 41-point evidence score and scorecard dimensions cannot be
   promoted by this fixture-only contract; strengthen the `python-hostlist`
   first-party metadata chain without changing any rights gate.
2. Freeze a new-version real host-lifecycle/raw-stress adapter and
   non-orthogonal numeric oracle, including finite resource limits and
   compatibility tests, while keeping workflow registration and dispatch
   disabled.
3. Only after dataset-specific private-use rights and every isolation,
   resource, OCI and solver gate close, seek separate human authorization for
   a private four-container 693-by-2 run. Aggregate publication, runtime
   redistribution and deployment remain separate default-deny decisions.

Only a future validated execution receipt may feed energy, force, stress,
errors and calibrated uncertainty into versioned World State/Observation and
the three-dimensional instrument. It still would not establish SOTA, leakage
certification, causal effects or industrial fitness without separate evidence.
