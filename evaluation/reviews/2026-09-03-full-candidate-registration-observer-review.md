# Full-candidate registration observer — independent review

Date: 2026-09-03

Disposition: **LIMITED GO for the bounded registration-only observer to enter
the ordinary commit and pull-request lifecycle**

Producer, dispatch, scientific promotion, release and deployment disposition:
**NO-GO**

No open P0 or P1 finding remains in the reviewed registration-only source.
This slice distinguishes the already registered quarantine workflow identity
from the still-unconfigured executable producer identity, and adds a bounded,
authenticated, GET-only GitHub observer plus a machine-readable abstaining
schema. It does not edit the registered workflow, configure a producer, create
or dispatch a run, read an artifact or log, access Random-TP, or run MatterSim
or MACE.

This record is itself source-scoped evaluator input. The frozen candidate
below is the complete implementation and generated-evidence tree immediately
before this file was added. To avoid circularly claiming its own digest, final
acceptance requires the post-inclusion evaluator, source-manifest, build and
review checks described below. Editing this record or any source after those
checks makes that evidence stale.

## Frozen bounded hypotheses and acceptance tests

The round froze three administrative hypotheses:

1. The registered quarantine shell can have its own exact, immutable identity
   without configuring or branding an executable full-candidate producer.
2. Two complete authenticated GitHub REST snapshots can fail closed on source,
   identity, pagination, selected current classic branch protection, Sentinel
   attempt-one or pass-to-pass drift while making no write request.
3. A strict `tf.atomistic-full-candidate-registration-observation/0.1`
   contract can expose only `verified-registration-only`, with all producer,
   dispatch, rights, handoff, scientific, comparison and promotion fields
   fixed to false, null or unavailable.

Acceptance required:

- the existing producer constant to remain `configured:false` with `id:null`,
  and the old readiness command to continue rejecting with
  `producer-workflow-not-pinned`;
- registration identity to bind workflow ID `349363715`, node ID
  `W_kwDOUG-2WM4U0t4D`, exact name/path/state, historical signed source commit,
  source tree/blob and independent source SHA-256;
- fixed `https://api.github.com` endpoints, GET only, no body, no redirects,
  bounded response size and time, strict UTF-8 and duplicate-key rejection,
  pinned API versions, validated pagination links and an explicit empty
  terminal page;
- two complete, stable passes over workflow, target-run, repository-run and
  Sentinel attempt-one job listings;
- exact current-main ancestry/blob continuity, selected current classic
  branch-protection fields, historical Sentinel run/job/check/check-suite
  bindings and zero currently visible target workflow runs;
- a strict schema and negative corpus for identity, source, pagination,
  protection, Sentinel, transport, JSON, timeout, token-redaction, producer
  provenance-brand and CI/evaluator-isolation failures;
- no change to `tf.world/0.3`, `tf.action/0.3`, `tf.observation/0.3`, the locked
  scorecard, the 520-byte quarantine workflow or any scientific result; and
- complete local lint, type, numerical, schema, manifest, deterministic
  evaluator, production-build, dependency and independent-review gates.

The main agent owned implementation and review-driven fixes but did not approve
the candidate. All approvals below came from read-only reviewers. Review
statements do not replace executable validation, an independent solver,
experiments or qualified engineering review.

## Live baseline and isolation

Work took place only in the isolated worktree
`/Users/tonywilliam/Documents/ChatGPT/Tailing Future-v05` on branch
`codex/v05-readonly-github-registration`. The unrelated dirty original
worktree was preserved.

- Local base/HEAD and observed GitHub main were
  `3221265a4145626dd9e32876fa911f23ae49fbff` before the candidate.
- Base tree was `2b9735696a4c2b1fc8419b6de818df7289246c8b`.
- Existing first-main Sentinel run `33760752864`, attempt 1, workflow
  `344526316`, job/check `100666166912` was successful for that exact main
  revision. This is prior control-plane evidence, not a new candidate run.
- The registered quarantine workflow remained active as GitHub workflow
  `349363715`; registration does not imply producer configuration or dispatch
  eligibility.
- The canonical Cloudflare site remained the prior deployment. No local build
  or candidate artifact was deployed in this round.

The final pre-record implementation/evidence freeze relative to base was:

- staged tree: `69473f46e96722c5d289475ec1cfcace7015ac35`;
- cached binary diff SHA-256:
  `9fb687f7c7c8388d33ab1a8547290369c664e21e94aa497e4acf24f5a2a3ee82`;
- 13 paths, 2,440 insertions and 32 deletions;
- zero unstaged and zero untracked paths; and
- clean cached diff check.

The implementation-only tree reviewed before generated reports and this
record was `78ac397b97d67be30a9111f7f6586fcea4978f2a`, with cached binary diff
SHA-256
`55d6c6a6c1ab7603749d9c5bdad259a30a8f46eb3d6fb6c065271cf39e4d1e83`.

## Implementation and machine-readable boundaries

The observer validates the historical source chain:

- commit `3221265a4145626dd9e32876fa911f23ae49fbff`, signed and verified by the
  GitHub response;
- parent `72bc2011d75d9880b9918b70c903129b9bf1de65`;
- tree `2b9735696a4c2b1fc8419b6de818df7289246c8b`;
- workflow blob `76d40b0938df50375728b4f68133a52a1ceabd13`;
- 520 source bytes with SHA-256
  `sha256:e578459f2c46e77d10f3fd944984daa01f845219921454c3095b9852e4074cc0`;
  and
- first-main Sentinel run/attempt/job/check/check-suite identity.

The successful schema fixes all six scientific/readiness claims to false,
all producer, dispatch, rights, private-handoff and actual-runner/toolchain
prerequisites to false, and MatterSim, MACE and comparison results to null with
status `unavailable`. Registration and producer objects cannot cross the
process-local provenance brands used by later policy.

`currentMain.branchProtection` means protection observed during the current
two-pass query. It is not proof of the protection configuration at the time of
the historical Sentinel run. Only selected classic-protection fields are
validated; `rulesetsFullyObserved` and `bypassActorsFullyObserved` remain
false. Similarly, a complete current visible listing is not an append-only
history: `completeHistoryProven`, `neverRunProven` and `noDeletionProven`
remain false. Local `observationWindow` timestamps are not GitHub or trusted
third-party timestamp evidence. Successful API reads do not prove that the
credential lacks broader permissions, so `credentialReadOnlyScopeProven`
remains false.

## Executable evidence

| Gate | Result | Boundary |
| --- | --- | --- |
| Final registration/workflow targeted tests | PASS: 4 files, 103/103 | Includes schema, protection, two-pass pagination, transport/JSON/time/token, readiness, private-handoff and CI isolation negatives |
| Full ESLint | PASS | Repository lint surface |
| TypeScript | PASS | `tsc --noEmit` after controlled report restoration |
| Full JavaScript clean run | PASS: 110 files; 2 environment skips; 1,084 tests; 5 skips; 0 failures | 542.74 seconds under the unchanged 20-second test timeout |
| Core Python | PASS: 91; 1 skipped | Available local environment |
| OpenMM policy/scientific tests | PASS: 42 | Stored assets remain non-reproduced |
| Safe ZIP tests | PASS: 3 | Duplicate-name warning was the expected negative fixture |
| Atomistic plan/runtime validators | PASS | `693×2 — NOT RUN · PLAN ONLY — NO INFERENCE`; runtime frozen but not scientifically reproduced |
| Deterministic evaluator | CONDITIONAL 41/100; `hardGateFailures: []` | Evidence maturity only; no SOTA or scientific-truth score |
| Independent source manifest | PASS: 393/393; zero path, order or digest mismatch | Aggregate `sha256:2b1cb981199feac326182868e592aa129661349412c48d1197fd9870931a3087` |
| Production build/isolation | PASS | 64 files; 5,549,854 bytes; digest `sha256:86f29a11adfcc7727fc3cd30022c2f87cd87dc81442abacef7867a1f2b7a5f7f`; 8 forbidden public fields absent |
| Dependency audit/tree | PASS | Zero reported vulnerabilities; `npm ls --all` reported no problems |
| Old full-candidate readiness | Expected rejection, exit 1 | `producer-workflow-not-pinned`; not converted into a pass |
| Pre-commit release manifest | Expected rejection, exit 1 | `release commit must be a full lowercase Git SHA`; no `.release-artifact` was created |

The clean JavaScript run followed one invalidated full-suite attempt in which
two unchanged long numerical tests exceeded the 20-second wall-clock timeout
under transient host contention. There was no numerical assertion or digest
failure. Both files then passed alone, 11/11 and 15/15, and both passed again
inside the complete unchanged suite without increasing the timeout.

An earlier software-review helper mistakenly started and interrupted a
shared-root evaluator, violating the mandatory read-only reviewer boundary and
disqualifying that helper's statement as final approval. Its orphaned worker
had removed the four public report outputs, so one subsequent `npm run check`
correctly stopped at TypeScript. The main agent waited for that worker to
finish, moved its abandoned snapshot recoverably to
`/tmp/tf-orphaned-evaluator.U5kKrx/`, regenerated all four reports with a
single controlled evaluator, and reran every applicable gate. Neither failed
attempt is counted as passing evidence. A separate software reviewer then
performed the final read-only review.

The latest controlled authenticated observer ran from
2026-09-03T16:02:41.065Z through 2026-09-03T16:02:52.795Z and independently
passed the new schema:

- current main: `3221265a4145626dd9e32876fa911f23ae49fbff`;
- registered workflow: `349363715`, state `active`;
- workflows: 7; current visible repository runs: 193;
- target workflow runs: 0; repository-wide target-path matches: 0;
- pages including explicit empty terminators: workflow 2, target-run 1,
  repository-run 3 and Sentinel-job 2;
- stable passes: 2; snapshot SHA-256
  `sha256:189e04c2f05db3fc5160045a2126238cf475631d1071f95568becc0dc42afe59`;
  and
- canonical observation output SHA-256
  `sha256:c878194a3eda2ee104b0e46db579dba86942126dc8480891df61c85fcc6b63d6`.

This is a current API-visible zero count, not proof that the workflow never ran
or that no historical run was deleted.

## Independent reviewers and findings

- Mechanism/scientific validity: Lagrange,
  `/root/scientific_registration_review`.
- Software, schema and evaluator integrity:
  `/root/software_runtime_review`.
- Dated primary-source/SOTA comparison and next gaps: Carver,
  `/root/sota_scout_round2`.

These three final reviewers were read-only on the candidate. The disqualified
helper incident is recorded above and was not treated as approval. The final
implementation freeze received registration-only GO/LIMITED GO with no open P0
or P1.

### P0

No P0 finding was recorded.

### P1 — closed

- The first software review found that required conversation resolution was
  not observed. The policy and schema now require
  `required_conversation_resolution.enabled === true`; false, missing, null
  and wrong-type mutations all reject.
- The first draft of this record contradicted the recorded helper incident by
  saying every reviewer had remained read-only. The helper is now explicitly
  disqualified from final approval, and the final software reviewer is the
  separate read-only `/root/software_runtime_review` agent.

### P2 — closed

- Two-pass and four-list pagination page counts are now projected into the
  schema, and readiness, real private-handoff and CI/evaluator separation have
  direct regression tests.
- Documentation now describes the token permissions as a conservative
  documented fine-grained read-permission union, not a universally proven
  least-privilege minimum.
- Documentation now distinguishes the future authenticated
  executable-producer/job GitHub adapter from the present registration-only
  observer.
- The first review-record draft named source categories without a source
  ledger. The ledger below now records each official URL, access date,
  evidence class and an honest null digest for unlocked web content.

The post-record freeze still requires each relevant reviewer to verify that
the only additions are this record and regenerated evaluator projections, and
that these closures remain exact.

## Dated primary-source boundary and residual gaps

The SOTA comparison for this administrative slice used only GitHub's official
REST and Actions documentation. Each external page is labelled `reference`,
not `reproduced`; `digest:null` records that live web content was not locked.

| Official source | Evidence class | Version/access basis | Digest |
| --- | --- | --- | --- |
| [Workflow syntax and event filters](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax) | `reference` | unversioned live documentation; accessed 2026-09-03 | `null` |
| [Fine-grained personal-access-token permissions](https://docs.github.com/en/rest/authentication/permissions-required-for-fine-grained-personal-access-tokens) | `reference` | unversioned live documentation; accessed 2026-09-03 | `null` |
| [Actions workflow REST endpoints](https://docs.github.com/en/rest/actions/workflows?apiVersion=2022-11-28) | `reference` | REST API `2022-11-28`; accessed 2026-09-03 | `null` |
| [Checks REST endpoints](https://docs.github.com/en/rest/checks/runs?apiVersion=2022-11-28) | `reference` | REST API `2022-11-28`; accessed 2026-09-03 | `null` |
| [Branch-protection REST endpoint](https://docs.github.com/en/rest/branches/branch-protection?apiVersion=2022-11-28) | `reference` | REST API `2022-11-28`; accessed 2026-09-03 | `null` |
| [Git blob REST endpoint](https://docs.github.com/en/rest/git/blobs?apiVersion=2022-11-28) | `reference` | REST API `2022-11-28`; accessed 2026-09-03 | `null` |
| [REST pagination](https://docs.github.com/en/rest/using-the-rest-api/using-pagination-in-the-rest-api?apiVersion=2022-11-28) | `reference` | REST API `2022-11-28`; accessed 2026-09-03 | `null` |

No external scientific performance number entered a ranking, and nothing in
this slice is labelled reproduced.

Residual limitations are explicit:

- GitHub run listings are mutable and no externally anchored append-only audit
  or trusted timestamp is present.
- Effective rulesets and bypass actors are not completely observed.
- Random-TP structure/redistribution rights, private handoff, actual producer
  runner/tool bytes and executable producer identity remain unverified.

The current champion remains v0.4.11's validated browser-side atomistic
dynamics foundation. This slice improves administrative provenance only; it
does not narrow the scientific gap to AIDO-style learned multimodal or
multiscale state, MatterSim/MACE inference, PFHub Benchmark 3, Cantera 3.2 CSTR
or DFT/electronic structure.

## Scientific claim boundary

This change introduces no energy, force, stress, uncertainty, throughput,
memory, material property, cross-scale bridge, mechanism, causal effect,
counterfactual or industrial recommendation. Counts are item counts, sizes are
bytes, digests are integrity identifiers, and the observation window is local
UTC process time; none is a physical quantity.

MatterSim and MACE remain external auditable references. The locked Random-TP
author-test benchmark remains 693 structures by 16 atoms, but neither model has
run the complete set. There is no local energy/force/stress result, error,
throughput, memory result, uncertainty, classical-potential comparison,
data-leakage certification or SOTA claim. The R2 reduced-unit
Lennard-Jones/heat/A-to-B world remains a reduced-unit demonstration, not a
real material, chemical mechanism, reactor or process model.

## Lifecycle state at record creation

- Local validation: complete for the pre-record tree.
- Commit and push: not performed.
- Pull request and candidate CI: not created/not run.
- Protected-main merge and first-main CI: not performed/not run for this
  candidate.
- Release artifact: not created or approved.
- Cloudflare deployment and post-deploy smoke: not performed; the canonical
  site still represents a prior main artifact.

No deployment is authorized for this registration-only slice. Any later
commit, PR, CI, merge or artifact must be reported as its own state and bound
to its exact revision.

## Next round — at most three tasks

1. Add externally anchored, append-only run/artifact deletion evidence and
   trusted-time evidence; either observe effective rulesets/bypass actors or
   continue machine-readable abstention.
2. Separately freeze and review Random-TP rights, the restricted private
   handoff, executable producer identity, and actual runner/toolchain bytes.
3. Only after those gates close, run the first MatterSim and MACE `693×2`
   campaign and have an independent verifier calculate complete energy, force,
   stress, error, throughput, memory and uncertainty evidence.
