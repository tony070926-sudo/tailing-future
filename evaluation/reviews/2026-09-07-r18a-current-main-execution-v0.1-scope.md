# R18a current-main execution — frozen bounded scope

Frozen by root on 2026-09-07 before Builder implementation. Builder: Astra.
The previous Goal turn made progress (admission preparation, two P1 closures,
full gates and an isolated compatibility diagnostic). The active Goal remains
R18a closure followed by full PFHub 7(a) spatial/time convergence; a causal
library migration is not substituted for that critical path.

## Hypothesis and boundary

Unchanged R18a successor-v3 solver/oracle code can materialize its existing
locked runtime independently in the current-main integration checkout, pass
the full 21 Python and 29 Node acceptance tests without skips, and generate
two byte-identical receipts equal to the frozen formal receipt. This is an
execution/migration hypothesis enabling the subsequent full convergence
experiment, not new physical validation or a complete release.

Keep the dimensionless PFHub-native basis, 8-by-4/two-step preflight domain,
equations, tolerances and all capability ceilings unchanged. Keep the three
tracks distinct: official-literal discrepancy, literal-BVP self-comparison,
and non-PFHub exact-trace strict MMS. Do not call this t=8 convergence.

## Verified starting identities

- Original source root: `/Users/tonywilliam/Documents/ChatGPT/Tailing Future`.
  Preserve every existing source/evidence byte; do not overwrite historical
  manifests, receipts, locks, reviews or tests.
- Integration root: `/Users/tonywilliam/Documents/ChatGPT/Tailing Future-backend-release-r18a`.
  Base `307e6d723086fe41b6d89e9f9b4b532364ccaeff`.
- Preserved admission v0.1 candidate: 697 canonical bytes,
  `e28fc818f7f765a92ea293ac3ee402013d1ad4a895676adf7482310ee5d06fec`.
  Completed review: 81228 bytes,
  `8ed096527bc1557a72fca40d0abce06726ca4a05c2936d2e26917243317231f7`.
- Root reverified both historical R18a sets and all member bytes this turn:
  v2 fourteen-file set `e99e24b1e67d41565438014f29dd7496aac4460322bad94548c694694d79f712`;
  v3 seventeen-file set `be645d99fb86ed8c9d278a9ea9c6884ccbed9a982b11b21987a408ada1ecf344`.
  All ten stored mutation observations match their raw TAP and registered
  semantic reasons. These checks do not count as new numerical execution.
- Frozen R18a lock: `cd7f8e8a47d67596c025a8035890e99f5a367be562289e84ae4f6d87e79c08ee`.
  Astra's read-only inspection verified 747 bootstrap, 2668 expanded wheel and
  3415 combined runtime records against this lock. Use the preserved private
  capsule as caller-supplied bootstrap; no new runtime lock is justified by
  the unrelated R17b shared-cache failure.

## Exact implementation scope

Import these twelve legacy-only files byte-identically:

- `scripts/mesoscale/pfhub7a_r18a_supervise_v3.py`
- `scripts/mesoscale/pfhub7a_r18a_worker.py`
- `scripts/mesoscale/pfhub7a_r18a_oracle.py`
- `scripts/mesoscale/pfhub7a_r18a_preflight_v3_test.py`
- `scripts/mesoscale/pfhub7a_r18a_schema_gate_v3.mjs`
- `scripts/mesoscale/pfhub7a_r18a_schema_gate_v3.node-test.mjs`
- `scripts/mesoscale/r16c_json_integrity.mjs`
- `schemas/pfhub7a-r18a-preflight-receipt-v0.3.schema.json`
- `evaluation/mesoscale/preregistrations/pfhub7a-r18a-independent-solver-preflight-v4-resolved.json`
- `evaluation/mesoscale/dependencies/pfhub7a-r18a-fipy/runtime-lock-v1.json`
- `evaluation/mesoscale/dependencies/pfhub7a-r17b-cas/benchmark7-aa4774ce.ipynb`
- `evaluation/mesoscale/fixtures/pfhub7a-r18a-independent-solver-preflight-v4-successor-v3-receipt.json`

Add a current-root numerical runner and focused runner test, a versioned v0.2
admission schema/ledger/checker/test, and an exact PFHub7 upstream notice plus
source-provenance record. Required source/review manifests and gate evidence
are additive under evaluation. Freeze an exact observed candidate path list
and modes/sizes/digests before independent review; no broad-directory admission.

Only three current-main files may be reconciled:

1. `.gitignore`: add the exact root `/.tf-runtime/` exclusion.
2. `package.json`: require the Node 29-test corpus in ordinary tests and expose
   the explicit `r18a:validate` numerical gate.
3. `vitest.config.ts`: relocate the unchanged v0.1 admission suite to a
   mandatory digest-verified historical snapshot executed by the successor
   suite. Missing inputs, child errors or skipped historical execution fail.

All other 446 main files, the four v0.1 candidate files and all seven v0.1
review artifacts remain byte-identical. Historical reconstruction uses fixed
main Git blobs plus the frozen eleven additions, not selected current files.
No frontend, 0.3 contract, evaluator, release-pipeline or scientific solver
changes are in this round. R12 stays excluded; no source is declared licensed
merely because it is absent. Preserve all 45 original conflicts as history;
these three current-main reconciliations do not silently resolve them.

## Acceptance frozen before implementation

1. The explicit numerical command requires caller-supplied bootstrap and
   wheelhouse paths; absent/invalid inputs, wrong platform, failed identity,
   partial/skipped tests, timeout or receipt mismatch return nonzero with a
   machine-readable abstention. No automatic download or dependency resolver.
   Check executable/runtime bytes before launching a caller-supplied Python;
   unverified import hooks or bytecode must not be executable authority.
2. Independently materialize the runtime under the integration root. Do not
   symlink that runtime to the old capsule or change the preserved capsule.
   Keep existing worker/supervisor child limits and numerical assertions.
   Aggregate local numerical acceptance budget: ten minutes. Exceeding it
   requires an explicit new disposition, never a silent timeout increase.
3. Run the unchanged Python corpus with all 21 tests and zero skips, Node
   corpus with all 29 tests and zero skips, strict Ajv/schema/semantic gate,
   syntax and lint. Execute the same supervisor command twice to separate
   exclusive output paths. Both generated receipt files must be 248778 bytes
   with SHA-256 `d91e31d261bf77f99b17e810f5f51aab131dd5339e281839933e45fa79855e5c`.
   Diagnose differences; never replace the expected identity automatically.
4. The Linux Sentinel currently uses Ubuntu 24.04/Python 3.12.11. Its ordinary
   Node/runner-contract tests are not a Darwin numerical run. Keep the explicit
   Darwin numerical gate separate and required for scientific promotion;
   missing inputs must fail it, not silently skip or produce a success receipt.
   No unsupported-platform or general CI success is asserted in this round.
5. Bind the new actual-root admission identity and exactly twelve imports,
   three main reconciliations, successor tools, notice and review paths. Test
   missing/extra files, unknown/mutated identities, ignored-source hiding,
   R12 insertion and historical-approval/scientific-claim promotion. The old
   17-file R18a identity is historical even if the executable receipt matches.
6. Retrieve and hash exact pinned PFHub notice/provenance bytes. Record the
   upstream NIST notebook and the 23-byte Markdown-heading-only difference
   from the selected aa4774ce notebook if independently confirmed. Preserve
   original notebook bytes, attribution and disclaimer. This is a scoped
   source-evidence record, not blanket legal clearance or NIST endorsement.
7. Freeze Builder diff/gates before independent read-only science, software/
   numerical/evaluator and dated primary-source SOTA reviews (at most three
   next scientific gaps). Builder cannot approve; root owns any P0/P1 fixes.
   Relevant reviewer must verify closure before any commit or promotion.
8. After the last fix, run all applicable repository gates on the frozen
   candidate. Evaluator-generating full checks run in complete verified
   disposable snapshots, with declared generated outputs retained as evidence;
   do not overwrite preserved integration reports. Recheck source identities.
   Write the dated final review with skips, warnings, limitations and raw logs.

## Continuation, not redefined completion

This round must yield an executable current-main R18a baseline, not just an
updated inventory. It does not complete the full active Goal. After its
independent closure, freeze and execute R18b full PFHub7(a) to t=8, with separate
spatial, temporal and algebraic-error controls and an independent discretization.
Keep same-equation matrix agreement distinct from independent-discretization
convergence and all engineering/causal/material claims unavailable until their
own evidence exists. Full integration, source notices and protected-main
release/deployment remain separate blocking requirements. No commit, push,
deployment, score increase or goal completion is authorized by this scope.
